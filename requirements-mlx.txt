# Apple Silicon MLX sidecar. Do not mix with requirements-ml.txt (Unsloth / CUDA).
# Install: pip install -r requirements-mlx.txt

mlx
mlx-lm
pyyaml
# Optional browser chat: pip install gradio  (demo/chat_gradio.py)
# Optional Hub download/publish: pip install huggingface_hub
